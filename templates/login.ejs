<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login - Game Library</title>
    <style>
        body { 
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh; display: flex; justify-content: center; align-items: center; 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .login-box {
            background: rgba(255,255,255,0.95);
            padding: 2rem 3rem;
            border-radius: 20px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.16);
            min-width: 320px;
            text-align: center;
        }
        .login-box h2 { color: #764ba2; margin-bottom: 1rem; }
        .login-box input { width: 100%; padding: 1rem; margin-bottom: 1rem; border-radius: 8px; border: 1px solid #ddd; }
        .login-box button { padding: 1rem 2rem; border: none; background: #667eea; color: #fff; border-radius: 8px; font-weight: bold; cursor: pointer;}
        .login-box .error { color: #ee5a52; margin-bottom: 1rem;}
    </style>
</head>
<body>
    <form class="login-box" method="POST">
        <h2>Login</h2>
        {% if error %}
        <div class="error">{{ error }}</div>
        {% endif %}
        <input type="text" name="username" placeholder="Username" required autofocus>
        <input type="password" name="password" placeholder="Password" required>
        <button type="submit">Sign In</button>
    </form>
</body>
</html>